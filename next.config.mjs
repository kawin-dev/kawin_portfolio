// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/kawin_portfolio', // Change this to your GitHub repository name
  assetPrefix: '/kawin_portfolio/', // Change this to your GitHub repository name
  output: 'export', // Specify static export
};

export default nextConfig;